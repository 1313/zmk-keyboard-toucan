CONFIG_ZMK_DISPLAY=y
# Blank the display when the keyboard goes idle (CONFIG_ZMK_IDLE_TIMEOUT, 30s by
# default) to save power. The custom sleep screen is rendered on the SLEEP
# transition (deep-sleep), which is independent of this setting.
CONFIG_ZMK_DISPLAY_BLANK_ON_IDLE=y
